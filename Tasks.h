#ifndef _TASKS_H_
#define _TASKS_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Graph.h"

void TaskOne(Graph** graph);
void TaskTwo(Graph** graph);
void TaskThree(Graph** graph);
void Bonus(Graph** graph);

#endif